.native/
zig-out/
.zig-cache/
# `tools/package-npm.mjs` stages the npm package here out of build
# output; the sources it assembles are `npm/` and `assets/`.
dist/
node_modules/
*.tgz

# logs
logs
*.log

# IntelliJ based IDEs
.idea

# Finder (macOS) folder config
.DS_Store
